server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#微服务作为消费方，OpenFeign想要使用Hystrix熔断机制，需要再yml文件中开启Hystrix (摸棱两可)
#feign:
#  hystrix:
#    enabled: true

